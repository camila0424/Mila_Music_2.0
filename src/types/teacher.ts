export interface Teacher {
  id: string;
  name: string;
  instrument: string;
  photo: string;
  language: string;
  ad_title: string;
  ad_desc: string;
  class_desc: string;
  number_of_classes: number;
  cost_per_hour: string;
}
